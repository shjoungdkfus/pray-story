import 'package:flutter/material.dart';

class AppColors {
  static const paper = Color(0xFFF5F0E1);         // 메인 종이색 (통일 기준)
  static const background = paper;                // 전체 배경 = 종이색
  static const card = paper;                      // 카드/칸 = 종이색
  static const searchBar = paper;                 // 검색바 = 종이색
  static const bottomBar = Color(0xFFEFE6D0);     // 하단바만 구분 (살짝 진한 베이지)
  static const textPrimary = Color(0xFF150A02);   // 진한 에스프레소 브라운
  static const accent = Color(0xFF8B1A0F);        // 깊은 레드 (가시성 강화)
  static const textHint = Color(0xFF7A6050);      // 중간 브라운 (계층 구분)
  static const divider = Color(0xFFC4B49A);       // 선명한 구분선
}
